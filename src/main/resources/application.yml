spring:

  datasource:
    url: jdbc:mysql://localhost:3306/springdb
    username: springuser
    password: spring123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

server:
  port: 8080